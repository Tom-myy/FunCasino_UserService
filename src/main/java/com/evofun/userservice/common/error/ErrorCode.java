package com.evofun.userservice.common.error;

public enum ErrorCode {
    VALIDATION_ERROR,
    AUTHORIZATION,
    GAME_RULE_VIOLATION,
    ALREADY_EXISTS,
    INVALID_CREDENTIALS,
    SYSTEM_ERROR,
    INVALID_REQUEST_FORMAT,
    FORBIDDEN,
    UNKNOWN_ERROR
}